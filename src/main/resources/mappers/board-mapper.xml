<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace= "BoardMapper">
	<resultMap type= "Board"				id="boardResultMap">
  		<id property="boardNo" 				column="BOARD_NO"/>
  		<result property="memberId" 		column="MEMBER_ID"/>
  		<result property="boardTitle" 		column="BOARD_TITLE"/>
  		<result property="boardContents"	column="BOARD_CONTENTS"/>
  		<result property="boardCategory" 	column="BOARD_CATEGORY"/>
  		<result property="boardDate" 		column="BOARD_DATE"/>
  		<result property="boardCount" 		column="BOARD_COUNT"/>
  		<result property="boardLike" 		column="BOARD_LIKE"/>
  		<result property="bFileName" 		column="B_FILE_NAME"/>
  		<result property="bFileRename" 		column="B_FILE_RENAME"/>
  		<result property="bFilePath" 		column="B_FILE_PATH"/>
  	</resultMap>
  	<insert id="insertBoard">
  		INSERT INTO BOARD_TBL VALUES(SEQ_BOARDNO.NEXTVAL,#{memberId}, #{boardTitle},#{boardContents},#{boardCategory},DEFAULT,DEFAULT,DEFAULT,#{bFileName},#{bFileRename},#{bFilePath})
  	</insert>
</mapper>











